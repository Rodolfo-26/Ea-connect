import 'package:flutter/material.dart';

class ArchitectChatScreen extends StatelessWidget {
  const ArchitectChatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Chat del Arquitecto'),
    );
  }
}
