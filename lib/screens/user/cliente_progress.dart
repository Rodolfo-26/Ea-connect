import 'package:flutter/material.dart';

class ClienteProgressScreen extends StatelessWidget {
  const ClienteProgressScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Progreso del cliente'),
    );
  }
}
