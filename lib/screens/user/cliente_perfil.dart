import 'package:flutter/material.dart';

class ClientePerfilScreen extends StatelessWidget {
  const ClientePerfilScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Cuenta del cliente'),
    );
  }
}
